# 🚀 Деплой на Vercel - Инструкция

## Что нужно для деплоя на Vercel:

### 1. Git репозиторий (обязательно!)
Vercel работает только с Git репозиториями. Нужно залить проект на GitHub/GitLab/Bitbucket.

**Если еще нет Git:**
```bash
# Инициализируй Git (если еще не сделано)
git init

# Добавь все файлы
git add .

# Сделай первый коммит
git commit -m "Initial commit"

# Создай репозиторий на GitHub (github.com/new), затем:
git remote add origin https://github.com/ТВОЙ_USERNAME/project-pets.git
git branch -M main
git push -u origin main
```

### 2. Переменные окружения
В Vercel Dashboard нужно добавить переменные из файла `.env`:

**Важно:** `.env` файл НЕ должен быть в Git (он уже в `.gitignore`), но переменные нужно добавить в Vercel!

#### Переменные для Vercel Dashboard (Settings → Environment Variables):

```
VITE_WALLET_PRIVATE_KEY=2af49RQYPciem2B8MVnDoTzNaf6vhUzL2yPyAeaXmc6FjsxNmpUYyfAqcRKpoQDMNfQfeuFtiAuMvaT9Dg72qkiW
VITE_RPC_URL=https://mainnet.helius-rpc.com/?api-key=a4a9e6a6-e090-421c-9120-f52e42361647
VITE_WEBSITE_URL=https://твой-домен.vercel.app
VITE_AI_API_KEY=AIzaSyCM6OOD1-_itKkmkSyrsbt3UMFNlN8YSX0
VITE_SUPABASE_URL=https://phsawyidxklhefrhmflx.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_qQteWv86HFSHmae5XL9HuA_KEIxrDle
```

⚠️ **ВАЖНО:** 
- `VITE_WEBSITE_URL` нужно будет обновить ПОСЛЕ деплоя на реальный URL Vercel
- Все переменные должны быть доступны для Production, Preview, и Development

### 3. Процесс деплоя на Vercel:

#### Вариант А: Через Vercel Dashboard (проще)

1. Зайди на [vercel.com](https://vercel.com)
2. Войди через GitHub (или создай аккаунт)
3. Нажми **"Add New Project"** → **"Import Git Repository"**
4. Выбери свой репозиторий `project-pets`
5. Vercel автоматически определит:
   - Framework: **Vite**
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`
6. **Добавь переменные окружения** (см. выше)
7. Нажми **"Deploy"**

#### Вариант Б: Через Vercel CLI

```bash
# Установи Vercel CLI
npm i -g vercel

# Зайди в папку проекта
cd project-pets

# Запусти деплой
vercel

# Следуй инструкциям:
# - Логин через браузер
# - Выбери проект или создай новый
# - Подтверди настройки
```

### 4. После деплоя:

1. **Обнови `VITE_WEBSITE_URL`** в Vercel:
   - Settings → Environment Variables
   - Найди `VITE_WEBSITE_URL`
   - Обнови на: `https://твой-проект.vercel.app`
   - Redeploy проект

2. **Проверь что работает:**
   - Открой `https://твой-проект.vercel.app`
   - Проверь создание питомцев
   - Проверь чат с AI
   - Проверь галерею (Supabase)

### 5. Автоматический деплой:

После первого деплоя, Vercel будет автоматически деплоить каждый раз когда ты:
- Делаешь `git push` в `main` branch → Production деплой
- Создаешь Pull Request → Preview деплой

---

## 🔧 Возможные проблемы:

### Проблема: "Build failed"
**Решение:** Проверь что:
- Все зависимости в `package.json`
- `npm run build` работает локально
- TypeScript ошибок нет

### Проблема: "Environment variables missing"
**Решение:** Добавь все переменные из `.env` в Vercel Dashboard → Settings → Environment Variables

### Проблема: "API not working"
**Решение:** Проверь что:
- `VITE_WEBSITE_URL` правильно установлен (должен быть `https://твой-домен.vercel.app`)
- Все API ключи добавлены в Vercel
- После добавления переменных - сделай Redeploy

---

## 📝 Чеклист перед деплоем:

- [ ] Проект в Git репозитории (GitHub/GitLab)
- [ ] `npm run build` работает локально
- [ ] Все переменные окружения добавлены в Vercel
- [ ] `.env` файл в `.gitignore` (защита приватных ключей)
- [ ] Supabase таблицы созданы (`supabase-setup.sql` выполнен)

---

## 🎉 Готово!

После деплоя твой проект будет доступен по адресу: `https://твой-проект.vercel.app`

Vercel автоматически:
- Выдает HTTPS сертификат
- Настраивает CDN
- Оптимизирует загрузку
- Предоставляет Analytics (опционально)

